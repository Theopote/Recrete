/**
 * Elevator feasibility threshold constants.
 *
 * IMPORTANT: These values are engineering placeholders and MUST be verified
 * against the applicable code原文 (e.g. GB 50763-2012) or confirmed by a
 * structural/MEP engineer before production use. Do not treat as authoritative.
 *
 * TODO: Replace placeholders after engineer/code review.
 */
export const ELEVATOR_SHAFT_MIN_WIDTH_M = 1.6;
export const ELEVATOR_SHAFT_MIN_DEPTH_M = 1.5;
export const ELEVATOR_LOBBY_MIN_DEPTH_M = 1.5;
/** Typical machine-room-less passenger elevator self-weight estimate (kg) */
export const ELEVATOR_TYPICAL_SELF_WEIGHT_KG = 4000;
